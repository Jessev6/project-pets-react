user nginx;
worker_processes 4;

events {
  worker_connections 2048;
}

http {
  sendfile     on;
  tcp_nopush   on;

  server {
    listen 80;

    location / {
      root   /usr/share/nginx/html;
      index  index.html;
      try_files $uri $uri/ /index.html;
    }
  }
}